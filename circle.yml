machine:
  node:
    version: 5.1.0

general:
  artifacts:
    - "dist/"
    - "package.json"

test:
  post:
    - npm run build-circle

notify:
  webhooks:
    - url: https://secure.sky.com/blackjack/builds/circleci
